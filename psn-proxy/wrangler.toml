name = "cals-trippin-psn-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# Optional: lock the proxy down to your app's origin instead of "*". Uncomment and set this,
# then redeploy with `wrangler deploy`.
# [vars]
# ALLOWED_ORIGIN = "https://chrisjamesseal.github.io"
